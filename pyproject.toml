[project]
name = "ppt-gen"
version = "0.1.0"
description = "AI PowerPoint Generator — prompt to editable .pptx via OOXML"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.36",
    "openai>=1.40",
    "pydantic>=2.7",
]

[tool.uv]
package = false
